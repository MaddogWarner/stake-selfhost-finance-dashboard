services:
  backend:
    volumes:
      - ../backend:/app
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload
  frontend:
    # In dev, run vite dev server instead - start separately with npm run dev.
