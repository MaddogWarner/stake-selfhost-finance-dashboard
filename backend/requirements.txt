fastapi==0.141.1
uvicorn[standard]==0.52.4
sqlalchemy[asyncio]==2.0.52
asyncpg==0.31.0
alembic==1.18.5
pydantic-settings==2.14.2
pydantic==2.13.5
redis[hiredis]==8.1.0
apscheduler==3.11.3
yfinance==1.7.0
requests-cache==1.3.3
httpx==0.28.1
python-dotenv==1.2.3
bcrypt==5.0.0
cryptography==50.0.0

# Runtime deps of stake==0.13.0 (installed with --no-deps in the Dockerfile to bypass
# its python-dotenv<0.14 pin). python-dotenv (above), pydantic (above) also satisfy it.
aiohttp==3.14.3
inflection==0.5.1
single-version==1.6.0

# Transitive-only, pinned to clear a security advisory rather than because the
# app imports it. msgpack arrives via requests-cache; GHSA-6v7p-g79w-8964 (high,
# out-of-bounds read) is fixed in 1.2.1. Remove this pin once a direct dependency
# requires >=1.2.1 on its own.
msgpack>=1.2.1
